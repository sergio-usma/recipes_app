<tr>
  <td><%= foods[:food].name %></td>
  <td><%= foods[:recipe].quantity %></td>
  <td><%= foods[:food].price * foods[:recipe].quantity %></td>
  <td class="d-flex justify-content-center"><%= recipe_actions(foods[:food], foods[:recipe]) %></td>
</tr>
